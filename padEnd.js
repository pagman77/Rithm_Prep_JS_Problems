function padEnd(str, num, pad){
  if (num < str.length){
    return str
  }

  if (pad === undefined) {
    while (str.length < num) {
      str += " ";
    }
  } else {


  }
return str
};

function repeat(){

}